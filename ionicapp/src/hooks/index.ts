/**
 * Centralized export for all hooks
 * This allows for cleaner imports: import { useAuth, useToast } from '../hooks'
 */

export { useAuth } from './useAuth';
export { useToast } from './useToast';
export { useSocket } from './useSocket';